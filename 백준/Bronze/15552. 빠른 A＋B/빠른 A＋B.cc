#include <iostream>
using namespace std;

int main()
{
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    int  N , a , b;
    int S = 0;
    cin >> N ;
    for (int i = 0; i < N; i++) {
        cin >> a >> b;
        S = a + b;
        cout << S <<"\n";
        S = 0;
  
    }
    }
 